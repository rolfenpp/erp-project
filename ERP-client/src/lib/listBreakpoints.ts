/** Max viewport width (px) at which list tables switch to “compact” column hiding. */
export const COMPACT_LIST_MAX_WIDTH_PX = 1410

/** Default debounce for search fields on list pages (ms). */
export const LIST_SEARCH_DEBOUNCE_MS = 200
